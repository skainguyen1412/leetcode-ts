class BacktrackingSolution {
    subsets(input: number[]) {
        // Co 2 tree cach tiep can bai nay
        // mot la include exclude
        // hai la chon next element

        // In ra subset tat ca cac to hop co the
        const result: number[][] = [];
        const path = [];

        function backTrack(arr: number[]) {}

        return result;
    }
}

const testBacktrack = new BacktrackingSolution();
testBacktrack.subsets([1, 2, 3]);
// [], [1],[1,2]
